const Discord = require('discord.js');

module.exports = {
  name: "love", 
  alias: ["amor"], 
  
execute (client, message, args){

    let user = message.mentions.users.first()
    if(!user) return message.channel.send("Debes mensionar a alguien!")
    const infi = new Discord.MessageEmbed()
    .setTitle(`**${message.author.username}**`)
    .setDescription(`<:cora:847680272212557855> **∞** <:cora:847680272212557855>`)
    .setColor("FF001F")
    
    if(user.id === message.author.id) return message.channel.send(infi)

    const embed1 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`\n :broken_heart: 1% :broken_heart:`)
    .setColor("FF001F")

    const embed2 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 2% :broken_heart:`)
    .setColor("FF001F")

    const embed3 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 3% :broken_heart:`)
    .setColor("FF001F")

    const embed4 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 4% :broken_heart:`)
    .setColor("FF001F")

    const embed5 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 5% :broken_heart:`)
    .setColor("FF001F")

    const embed6 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 6% :broken_heart:`)
    .setColor("FF001F")

    const embed7 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 7% :broken_heart:`)
    .setColor("FF001F")

    const embed8 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 8% :broken_heart:`)
    .setColor("FF001F")

    const embed9 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 9% :broken_heart:`)
    .setColor("FF001F")

    const embed10 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 10% :broken_heart:`)
    .setColor("FF001F")

    const embed11 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 11% :broken_heart:`)
    .setColor("FF001F")

    const embed12 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 12% :broken_heart:`)
    .setColor("FF001F")

    const embed13 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(` \n :broken_heart: 13% :broken_heart:`)
    .setColor("FF001F")

    const e14 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 14% :broken_heart:`)
    .setColor("FF001F")

    const e15 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 15% :broken_heart:`)
    .setColor("FF001F")

    const e16 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 16% :broken_heart:`)
    .setColor("FF001F")

    const e17 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 17% :broken_heart:`)
    .setColor("FF001F")

    const e18 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 18% :broken_heart:`)
    .setColor("FF001F")

    const e19 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 19% :broken_heart:`)
    .setColor("FF001F")

    const e20 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 20% :broken_heart:`)
    .setColor("FF001F")

    const e21 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 21% :broken_heart:`)
    .setColor("FF001F")

    const e22 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 22% :broken_heart:`)
    .setColor("FF001F")

    const e23 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 23% :broken_heart:`)
    .setColor("FF001F")

    const e24 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 24% :broken_heart:`)
    .setColor("FF001F")

    const e25 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 25% :broken_heart:`)
    .setColor("FF001F")

    const e26 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 26% :broken_heart:`)
    .setColor("FF001F")

    const e27 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 27% :broken_heart:`)
    .setColor("FF001F")

    const e28 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 28% :broken_heart:`)
    .setColor("FF001F")

    const e29 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 29% :broken_heart:`)
    .setColor("FF001F")

    const e30 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 30% :broken_heart:`)
    .setColor("FF001F")

    const e31 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 31% :broken_heart:`)
    .setColor("FF001F")

    const e32 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 14% :broken_heart:`)
    .setColor("FF001F")

    const e33 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 33% :broken_heart:`)
    .setColor("FF001F")

    const e34 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 34% :broken_heart:`)
    .setColor("FF001F")

    const e35 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 35% :broken_heart:`)
    .setColor("FF001F")

    const e36 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 36% :broken_heart:`)
    .setColor("FF001F")

    const e37 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 37% :broken_heart:`)
    .setColor("FF001F")

    const e38 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 38% :broken_heart:`)
    .setColor("FF001F")

    const e39 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 39% :broken_heart:`)
    .setColor("FF001F")

    const e40 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 40% :broken_heart:`)
    .setColor("FF001F")

    const e41 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 41% :broken_heart:`)
    .setColor("FF001F")

    const e42 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 42% :broken_heart:`)
    .setColor("FF001F")

    const e43 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 43% :broken_heart:`)
    .setColor("FF001F")

    const e44 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 44% :broken_heart:`)
    .setColor("FF001F")

    const e45 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 45% :broken_heart:`)
    .setColor("FF001F")

    const e46 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 46% :broken_heart:`)
    .setColor("FF001F")

    const e47 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 47% :broken_heart:`)
    .setColor("FF001F")

    const e48 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 48% :broken_heart:`)
    .setColor("FF001F")

    const e49 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:broken_heart: 49% :broken_heart:`)
    .setColor("FF001F")

    const e50 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 50% :sparkling_heart:`)
    .setColor("FF001F")

    const e51 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 51% :sparkling_heart:`)
    .setColor("FF001F")

    const e52 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 52% :sparkling_heart:`)
    .setColor("FF001F")

    const e53 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 53% :sparkling_heart:`)
    .setColor("FF001F")

    const e54 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 54% :sparkling_heart:`)
    .setColor("FF001F")

    const e55 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 55% :sparkling_heart:`)
    .setColor("FF001F")

    const e56 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 56% :sparkling_heart:`)
    .setColor("FF001F")

    const e57 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 57% :sparkling_heart:`)
    .setColor("FF001F")

    const e58 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 58% :sparkling_heart:`)
    .setColor("FF001F")

    const e59 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 59% :sparkling_heart:`)
    .setColor("FF001F")

    const e60 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 60% :sparkling_heart:`)
    .setColor("FF001F")

    const e61 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 61% :sparkling_heart:`)
    .setColor("FF001F")
    
    const e62 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 62% :sparkling_heart:`)
    .setColor("FF001F")

    const e63 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 64% :sparkling_heart:`)
    .setColor("FF001F")

    const e64 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 64% :sparkling_heart:`)
    .setColor("FF001F")

    const e65 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 65% :sparkling_heart:`)
    .setColor("FF001F")

    const e66 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 66% :sparkling_heart:`)
    .setColor("FF001F")

    const e67 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 67% :sparkling_heart:`)
    .setColor("FF001F")

    const e68 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 68% :sparkling_heart:`)
    .setColor("FF001F")

    const e69 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 69% :sparkling_heart:`)
    .setColor("FF001F")

    const e70 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 70% :sparkling_heart:`)
    .setColor("FF001F")
    
    const e71 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 71% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e72 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 72% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e73 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 73% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e74 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 74% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e75 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 75% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e76 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 76% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e77 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 77% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e78 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 78% :sparkling_heart:`)
    .setColor("FF001F")
  
    const e79 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:sparkling_heart: 79% :sparkling_heart:`)
    .setColor("FF001F")

    const e80 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 80% :heart:`)
    .setColor("FF001F")
  
    const e81 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 81% :heart:`)
    .setColor("FF001F")
  
    const e82 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 82% :heart:`)
    .setColor("FF001F")
  
    const e83 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 83% :heart:`)
    .setColor("FF001F")
  
    const e84 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 84% :heart:`)
    .setColor("FF001F")
  
    const e85 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 85% :heart:`)
    .setColor("FF001F")
  
    const e86 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 86% :heart:`)
    .setColor("FF001F")
  
    const e87 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 87% :heart:`)
    .setColor("FF001F")
  
    const e88 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 88% :heart:`)
    .setColor("FF001F")
  
    const e89 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 89% :heart:`)
    .setColor("FF001F")
  
    const e90 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 90% :heart:`)
    .setColor("FF001F")
  
    const e91 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 91% :heart:`)
    .setColor("FF001F")
  
    const e92 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 92% :heart:`)
    .setColor("FF001F")
  
    const e93 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 93% :heart:`)
    .setColor("FF001F")
  
    const e94 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 94% :heart:`)
    .setColor("FF001F")
  
    const e95 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 95% :heart:`)
    .setColor("FF001F")
  
    const e96 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 96% :heart:`)
    .setColor("FF001F")
  
    const e97 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`97% [█████████.]`)
    .setColor("FF001F")
  
    const e98 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 98% :heart:`)
    .setColor("FF001F")
  
    const e99 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 99% :heart:`)
    .setColor("FF001F")
  
    const e100 = new Discord.MessageEmbed()
    .setTitle(`${message.author.username} & ${user.username}`)
    .setDescription(`:heart: 100% :heart:`)
    .setColor("FF001F")
  
  
  



    const embeds = [embed1, embed2, embed3, embed4, embed5, embed6, embed7, embed8, embed9, embed10, embed11, embed12, embed13, e14, e15, e16, e17, e18, e19, e20, e21, e22, e23, e24, e25, e26, e27, e28, e29, e30, e31, e32, e33, e34, e35, e36, e37, e38, e39, e40, e41, e42, e43, e44, e45, e46, e47, e48, e49, e50, e51, e52, e53, e54, e55, e56, e57, e58, e59, e60, e61, e62, e63, e64, e65, e66, e67, e68, e69, e70, e71, e72, e73, e74, e75, e76, e77, e78, e79, e80, e81, e82, e83, e84, e85, e86, e87, e88, e89, e90, e91, e92, e93, e94, e95, e96, e97, e98, e99, e100]
    let embedFinal = embeds[Math.floor(Math.random() * embeds.length)]
    message.channel.send(embedFinal)
  

 }

} 